services:
  db:
    image: "postgres"
    restart: "always"
    container_name: "do-it-db"
    env_file: ".env"
    ports:
      - "5433:5432"
    volumes:
      - "do-it-pgdb_data:/var/lib/postgresql/data"

networks:
  overlay:
    driver: "bridge"

volumes:
  do-it-pgdb_data:
